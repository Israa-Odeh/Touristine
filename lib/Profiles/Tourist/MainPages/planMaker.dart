import 'package:flutter/material.dart';

class PlanMakerPage extends StatelessWidget {
  const PlanMakerPage();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Plan Maker Section'),
    );
  }
}