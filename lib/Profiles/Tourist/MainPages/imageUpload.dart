import 'package:flutter/material.dart';

class ImagesUploadPage extends StatelessWidget {
  const ImagesUploadPage();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Images Upload Section'),
    );
  }
}